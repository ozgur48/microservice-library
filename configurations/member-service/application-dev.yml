eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
spring:
  kafka:
    bootstrap-servers: localhost:9094
  cloud:
    stream:
      kafka:
        binder:
          brokers: localhost:9094
      bindings:
        memberCreated-out-0:
          destination: member-topic
          contentType: application/json
          group: member-service
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8585/realms/turkcell-library-p1-dev

  datasource:
    url: jdbc:postgresql://localhost:5438/member_service
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: member_service_db_password

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
